#pragma once

namespace BF6HighPolyControlBridge
{
	// Starts the same direct-Steam read/build path as the High Poly radial's
	// BUILD action.  This is an editor automation seam, not a second importer.
	bool StartFullBuild();
}
